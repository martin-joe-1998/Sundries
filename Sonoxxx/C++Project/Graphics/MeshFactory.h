#pragma once
#include <memory>
#include "Mesh.h"
#include "Vertex.h"

namespace sl
{
  class MeshFactory
  {
  public:
    // éläpå`
    //static std::shared_ptr<Mesh> CreateQuad(ID3D11Device* device, float width, float height, const Vector4& color);

    //// â~
    //static std::shared_ptr<Mesh> CreateCircle(ID3D11Device* device, float radius, int segments, const Vector4& color);

  };
}
